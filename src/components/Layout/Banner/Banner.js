import React from "react";
import styles from "./Banner.module.css";

const Banner = () => {
  return (
    <div>
      <img src="/images/banner.png" alt="#" className={styles.banner} />
    </div>
  );
};

export default Banner;
